{
  "name": "golima",
  "version": "0.1.0",
  "private": true,
  "author": {
    "name": "Vincent To",
    "email": "gaconkzk@gmail.com",
    "url": "https://theflies.dev"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/electron-builder",
      "**/electron-builder/**",
      "**/electron-webpack",
      "**/electron-webpack/**"
    ]
  },
  "engines": {
    "yarn": "^1.22.2"
  },
  "scripts": {
    "start": "lerna run --stream start",
    "build": "lerna run build --scope frontend",
    "test": "lerna run test --scope frontend",
    "eject": "lerna run eject --scope frontend",
    "electron-dev": "lerna run --scope frontend electron-dev",
    "electron-pack": "lerna run electron-pack --scope frontend"
  },
  "devDependencies": {
    "lerna": "^3.21.0"
  }
}
