{
  "name": "lernago",
  "version": "0.1.0",
  "private": true,
  "author": {
    "name": "Vincent To",
    "email": "gaconkzk@gmail.com",
    "url": "https://theflies.dev"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/electron-builder",
      "**/electron-builder/**",
      "**/electron-webpack",
      "**/electron-webpack/**"
    ]
  },
  "engines": {
    "yarn": "^1.22.0"
  },
  "scripts": {
    "start": "lerna run --stream start",
    "build": "lerna run build --scope golima",
    "test": "lerna run test --scope golima",
    "eject": "lerna run eject --scope golima",
    "electron-dev": "lerna run --scope golima electron-dev",
    "electron-pack": "lerna run electron-pack --scope golima"
  },
  "devDependencies": {
    "lerna": "^3.21.0"
  }
}
